# MarveLens project

## First of all

Install dependencies:

```bash
cd marve-lens
npm i
```

## Then

Running in dev mode:

```bash
npm start
```

Go to `http://localhost:3000`

Build and serve:

```bash
npm run build
```

Go to `http://localhost:3001`